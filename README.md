# visualizador-perfil-github

Endpoints que vamos usar 

perfil: GET /users/:username

repositórios: GET /users/:username/repos (usar query per_page=10 e sort=updated / sort=pushed conforme o objetivo)